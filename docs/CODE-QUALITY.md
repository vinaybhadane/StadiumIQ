# Code Quality Standards

- All Python code conforms to PEP 8 standards enforced by Ruff.
- Strictly type checked via `mypy --strict` with zero generic `any` values.
- Frontend utilizes TypeScript strict checking modes to prevent undefined bindings.
