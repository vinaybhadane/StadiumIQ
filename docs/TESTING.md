# Testing Document

- **Backend Unit Tests**: Cover pure logic (egress speeds, rest constraints).
- **Backend Integration Tests**: Mock endpoint routers and GCP service fallbacks.
- **Frontend Vitest Tests**: Match lists, scheduling inputs, and metric changes.
