# Testing Strategy

- **Pytest**: Integration and unit testing checking logic fallbacks and validations.
- **Vitest**: Frontend component validations tracking UI inputs, states, and accessibility properties.
- **Gates**: Coverage limits enforced at 90% (backend) and 85% (frontend) in checking cycles.
